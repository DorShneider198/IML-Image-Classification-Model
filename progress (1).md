# סיכום התקדמות — אתגר סיווג ציפורים

עדכון אחרון: אחרי ריצת הבסיס המלאה (30 epochs) על Kaggle.

---

## מה המשימה

לבנות רשת שמזהה 20 סוגי ציפורים מתוך תמונה.

שני אילוצים שקובעים הכל:

**אימון מאפס.** אסור מודל מאומן מראש ואסור דאטה חיצוני. הרשת מתחילה ממשקולות אקראיות ורואה רק את 20,000 התמונות שקיבלנו.

**חצי מהציון הוא חסינות.** מבחן הבוחן מחולק שווה בשווה: חצי תמונות רגילות, חצי תמונות ששונו. השינויים בבחינה הם כאלה שלא נראה מראש.

המשמעות: מודל שמגיע ל-85 אחוז על נקיות ונופל ל-35 על מעוותות מקבל 60. מודל חלש יותר אבל יציב, 70 ו-65, מנצח אותו.

---

## הדאטה

20 מחלקות, 1000 תמונות לכל אחת. סה"כ 20,000, בערך 1.8 ג'יגה.

מקומית ב-`~/bird_data/train`, מחוץ ל-iCloud. הנתיב דרך משתנה סביבה `BIRD_DATA_ROOT`.

ב-Kaggle: `/kaggle/input/datasets/dorshneider/bird-data`.

בנוסף יש תיקיית augmentations עם שלושה סוגי עיוות, אבל **רק 20 תמונות לכל מחלקה** — מעט מדי לשימוש רציני.

---

## החלטה 1: פיצול הדאטה ✅

לפני הכל צריך דרך למדוד. **80 אחוז אימון, 20 ולידציה.** 16,000 מול 4,000.

למה 20 אחוז: הרעש במדידה תלוי בכמות המוחלטת של תמונות הבדיקה, לא באחוז. 4,000 נותנות אי-ודאות של פחות מאחוז. מצד שני הירידה מ-1000 ל-800 תמונות אימון למחלקה כמעט לא מורגשת.

שלוש החלטות בתוך הפיצול:

**מפצלים כל מחלקה בנפרד (stratified).** אחרת יוצא במקרה שמחלקה אחת מקבלת 187 תמונות בדיקה ואחרת 214.

**seed קבוע.** בלי seed כל הרצה מגרילה פיצול אחר, ואז כשמשווים שני מודלים משווים גם שני סטי בדיקה שונים. עם seed קבוע אותן 4,000 תמונות נשארות בבדיקה לתמיד, וכל הפרש מגיע מהמודל בלבד.

**ממיינים לפני שמערבבים.** סדר הקבצים ממערכת ההפעלה לא מובטח. בלי מיון, אותו seed יכול לתת פיצול שונה במחשב אחר.

**התוצר:** `split_data.py`, פלט קפוא ב-`split.json`. נתיבים בלבד, אף תמונה לא זזה.

---

## החלטה 2: מד החסינות ✅

צריך מספר שאומר כמה המודל נשבר כשמעוותים תמונה. תיקיית augmentations לא מספיקה — 20 תמונות למחלקה זה כלום.

**הפתרון: כותבים את שלושת העיוותים בקוד** ומפעילים על כל 4,000 תמונות הוולידציה. מקבלים שלושה סטי בדיקה בגודל מלא.

התיקייה שהורדנו משמשת רק לכיול:

| עיוות | אצלהם | אצלנו |
|---|---|---|
| black_white | — | זהה עד 0.13/255 |
| salt_pepper | 2.8% מהפיקסלים | 3% |
| color_jitter | בהירות 0.76-1.15, ניגודיות 0.67-1.31, רוויה 0.68-1.31, גוון ±0.01 | אותם טווחים |

**התוצר:** `data.py` — Dataset, transforms, שלושת העיוותים, DataLoaders.

אילוץ: הנרמול חייב להיות זהה בדיוק ל-evaluate.py. Resize 256, CenterCrop 224, ונרמול ImageNet. נרמול אחר פירושו שהמודל מקבל בבחינה תמונות בסקאלה שהוא לא מכיר, ונופל.

---

## החלטה 3: הארכיטקטורה ✅

**ResNet קטן, 2.8 מיליון פרמטרים.**

למה קטן ולא גדול. בדרך כלל רשת גדולה יותר עדיפה, אבל לא כאן: 16,000 תמונות ואתחול אקראי. רשת עם עשרות מיליוני פרמטרים תשנן את הדאטה — 99 אחוז על האימון, 40 על הוולידציה.

**מה זה skip connection.** כל בלוק לומד רק את התיקון על מה שהגיע אליו, במקום לבנות מחדש את כל הייצוג. בלי זה רשת עמוקה מאבדת אות בדרך אחורה ופשוט לא מתאמנת.

למה זה מתאים כאן: הכוח של ResNet הוא בעומק ולא ברוחב. הרבה שלבי עיבוד עם מעט פרמטרים. וזה מה שדרוש כדי להבחין בין מקאו לקקדו — שלבים שמזקקים צורה, לא שכבה ענקית שמשננת.

**המבנה:**

```
stem     3x3 conv, 32 ערוצים, stride 1     224x224
stage 1  2 בלוקים, 32 ערוצים,  stride 2    112x112
stage 2  2 בלוקים, 64 ערוצים,  stride 2     56x56
stage 3  2 בלוקים, 128 ערוצים, stride 2     28x28
stage 4  2 בלוקים, 256 ערוצים, stride 2     14x14
head     AvgPool -> Dropout 0.2 -> Linear(256, 20)
```

---

## ניסוי 0: הבסיס ✅

**30 epochs, בלי שום עיוות באימון.** רק חיתוך אקראי והיפוך.

| סט | דיוק | נפילה מהנקי |
|---|---|---|
| נקי | 79.97% | — |
| color_jitter | 76.15% | 4 נקודות |
| salt_pepper | 43.43% | 37 נקודות |
| black_white | 34.60% | 45 נקודות |
| **ממוצע מעוות** | **51.39%** | |

**ציון משוער בבחינה: כ-65.** (חצי נקי, חצי מעוות)

עלות: 91 שניות ל-epoch על T4, בערך 50 דקות סה"כ.

### על העקומה: כמה epochs באמת צריך

הדיוק עלה בהתמדה עד epoch 20 בערך, ואז זחל: 77.2, 77.8, 78.1, 78.4, 79.3, 79.5, 80.0, ואז נתקע.

**מסקנה: 30 זה בערך המספר הנכון.** להריץ 50 ייתן שבריר אחוז.

הערה: ה-cosine schedule מוריד את קצב הלמידה עד אפס בסוף, אז מספר ה-epochs הוא לא רק "כמה זמן" אלא חלק מהאימון עצמו. אי אפשר לעצור באמצע ריצה ארוכה ולקבל את התוצאה של ריצה קצרה — ברגע העצירה קצב הלמידה עוד גבוה והרשת לא התיישבה.

---

## שתי הכשלים של המודל, ולמה הם קורים

זה הלב של הפרויקט. הבסיס נועד בדיוק כדי לחשוף אותם.

### כשל 1: המודל חי על צבע

שחור-לבן מוריד 45 נקודות. כמעט חצי מהיכולת.

**למה זה קורה.** תסתכל על רשימת המחלקות: פלמינגו ורוד, כפן ורדרד, מקאו אדום-כחול, ברבור שחור שחור, טוקן עם מקור צבעוני. הצבע פותר חלק גדול מהמשימה כמעט לבד.

הרשת לא "בוחרת" להשתמש בצבע. במהלך האימון היא מחפשת כל דבר שמנבא את התווית, והצבע פשוט עובד מצוין על תמונות צבעוניות נקיות. אז המשקולות שמזהות ורוד מתחזקות, והמשקולות שמזהות צורת מקור מתחזקות פחות — כי הן פחות נחוצות.

זה קיצור דרך. הוא עובד מושלם עד הרגע שמורידים את הצבע, ואז לא נשאר כלום.

**ושים לב:** הפער החמיר עם האימון. אחרי epoch אחד הוא היה 22 נקודות, אחרי 30 הוא 45. ככל שמתאמנים יותר על תמונות צבעוניות, ההישענות על צבע מתחזקת.

### כשל 2: רעש נכנס לרשת כאילו היה קצוות

salt_pepper מוריד 37 נקודות, וזה יותר ממה שציפינו.

**למה זה קורה.** הפיקסלים המקולקלים הם רק 3 אחוז מהתמונה, נשמע זניח. אבל כל פיקסל כזה הוא קפיצה חדה מאוד — משחור מוחלט ללבן מוחלט מול השכן שלו.

השכבות הראשונות של רשת קונבולוציה מגיבות בדיוק לשינויים חדים, כי זה מה שמזהה קצוות וקווי מתאר. אז הרעש נכנס אליהן כאילו היה קצה אמיתי. השכבה הראשונה מייצרת אלפי "קצוות" מזויפים, והם מזהמים את כל מה שבא אחר כך.

### למה color_jitter כמעט לא מזיק

ארבע נקודות בלבד. הוא מזיז גוונים אבל שומר על היחסים ביניהם — ורדרד נשאר ורדרד גם אם קצת יותר כתום. הקיצור של הצבע עדיין עובד.

---

## העיקרון: איך משיגים חסינות

לא קיימת חסינות לכל שינוי אפשרי. מה שקיים זה משהו אחר.

רשת שראתה מגוון רחב של עיוותים באימון מפתחת נטייה להתעלם מהמראה השטחי ולהיאחז בצורה — מקור, צוואר, פרופורציות, קווי מתאר. וזו התכונה שמעבירה גם לעיוותים שלא ראתה.

**המנגנון:** אם בכל epoch התמונה מגיעה בצבע אחר, או לפעמים בלי צבע בכלל, אז הצבע כבר לא מנבא את התווית באופן אמין. המשקולות שנשענו עליו מפסיקות להשתלם, והרשת נאלצת לחזק את מה שכן נשאר קבוע — הצורה.

זה לא שאנחנו מלמדים אותה להתעלם. אנחנו **הורסים את הקיצור** ולא משאירים לה ברירה.

**ולכן מייצרים את העיוותים בקוד ולא בקבצים.** בתיקייה שהורדנו יש עותק אחד קבוע לכל תמונה — רעש שכבר הוגרל וקפא. אימון על זה מאפשר לרשת לשנן את התמונה המרועשת. כשהעיוות מיוצר בקוד, כל epoch מגריל מחדש, והרשת רואה אינסוף גרסאות.

---

## מבנה הבדיקה: מניפולציה מוחזקת

**הבעיה:** אם נאמן על שלושת העיוותים ונבדוק על שלושתם, נקבל מספר יפה שלא מנבא כלום. הרשת פשוט למדה את מה שראתה. והבחינה משתמשת בעיוותים אחרים.

**הפתרון: משאירים אחד בצד ולא נוגעים בו באימון בכלל.**

**הנבחר: salt_pepper.**

למה דווקא הוא: שחור-לבן ושינוי צבע משנים ערכי פיקסלים בצורה חלקה, בעוד רעש הורס פיקסלים בודדים. זה מנגנון שונה לגמרי. הצלחה עליו אחרי אימון על השניים האחרים היא הראיה החזקה ביותר לחסינות אמיתית.

**זה המספר היחיד שמנבא את הציון בבחינה.**

---

## טעויות שתפסנו בדרך

**color_jitter קבוע במקום מפוזר.** בהתחלה העיוות תמיד דחף לאותו כיוון — בהיר וחם יותר. זה אומר שמדדנו נקודה אחת בלבד. אם המודל מסתדר עם "בהיר" ומתרסק על "כהה", לא היינו רואים את זה בכלל. עכשיו הפקטורים נגזרים מ-hash של הנתיב: מפוזרים לשני הכיוונים, אבל קבועים לכל תמונה.

**חשבנו להשתמש בתיקיית augmentations כדאטה.** בפועל יש בה רק 20 תמונות למחלקה, ומהן רק חלק שייכות לצד הוולידציה. היא הפכה לכלי כיול בלבד.

**שלב 1 ברזולוציה מלאה.** בהתחלה גם שלב 1 רץ ב-224. עלות החישוב גדלה עם שטח התמונה, אז שכבה ב-224 עולה פי ארבע מאותה שכבה ב-112. זה עלה 9.3 דקות ל-epoch. הורדנו את שלב 1 ל-stride 2 והזמן ירד ל-2.9. **אבל השארנו את ה-stem ברזולוציה מלאה** — כך השכבה הראשונה עדיין קוראת תמונה חדה.

**הדאטה ב-iCloud.** כל קריאת תמונה עברה דרך שכבת סנכרון. הועבר החוצה.

**חשבנו לעבוד מקומית.** 455 שניות ל-epoch על MPS מול 91 על T4 ב-Kaggle. פי חמישה.

---

## הסדר קדימה

1. ✅ פיצול הדאטה
2. ✅ צינור נתונים ומדדי חסינות
3. ✅ ארכיטקטורה
4. ✅ **ניסוי 0: בסיס.** 80.0 נקי, 51.4 מעוות
5. ⬜ **ניסוי 1: grayscale + color jitter באימון.** salt_pepper נשאר מוחזק
6. ⬜ ניסויי כיוונון: עוצמה, אולי עוד סוגי עיוות
7. ⬜ ריצה סופית, README, הגשה

**מה מצפים לראות בניסוי 1:** הדיוק הנקי יירד מעט (אולי ל-76). black_white יקפוץ חזק. **והשאלה המכריעה: מה יקרה ל-salt_pepper.** אם הוא יעלה מ-43 בלי שאימנו עליו כלל — זו הראיה שהמודל למד להתעלם ולא רק שינן.

---

## שיטת עבודה

**משנים דבר אחד בכל ריצה.** הטעות הנפוצה היא לשנות שלושה דברים, לראות שיפור, ולא לדעת מה עזר.

**מתעלמים משיפורים קטנים.** 71 מול 72 זה בתוך הרעש. רק קפיצות של כמה אחוזים אמיתיות.

**סינון זול:** לבדיקה אם רעיון הולך לכיוון הנכון מריצים 10 epochs במקום 30.

---

## תזכורות טכניות

- `BIRD_DATA_ROOT` חייב להיות מוגדר, אחרת כלום לא רץ
- ב-Kaggle: הקוד מועתק ל-`/kaggle/working/project` כי `/kaggle/input/` היא לקריאה בלבד
- **להוריד את `weights.joblib` בסוף כל ריצה.** הסשן נמחק
- הסשן מת אחרי כ-20 דקות עם חלון סגור — להשאיר את הטאב פתוח
- ההגשה: `train.py`, `model.py`, `predict.py`, `weights.joblib`, `README`. לא נוגעים ב-predict.py
- ה-README דורש פירוט של המניפולציות ואיך השפיעו — המסמך הזה מכסה חלק גדול
- יש ראיון בסוף. שלושת הרעיונות שיישאלו עליהם: שני הכשלים של המודל, איך משיגים חסינות, ולמה מניפולציה מוחזקת
