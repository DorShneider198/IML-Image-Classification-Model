# סיכום התקדמות — אתגר סיווג ציפורים

## מה המשימה

לבנות רשת שמזהה 20 סוגי ציפורים מתוך תמונה.

שני אילוצים שקובעים הכל:

**אימון מאפס.** אסור להשתמש במודל מאומן מראש ואסור דאטה חיצוני. הרשת מתחילה ממשקולות אקראיות ורואה רק את 20,000 התמונות שקיבלנו.

**חצי מהציון הוא חסינות.** מבחן הבוחן מחולק שווה בשווה: חצי תמונות רגילות, חצי תמונות שעברו שינוי. שינוי צבע, תאורה, רקע, רעש. השינויים בבחינה הם כאלה שלא נראה מראש.

המשמעות המעשית: מודל שמגיע ל-85 אחוז על תמונות נקיות ונופל ל-35 על מעוותות מקבל ציון 60. מודל חלש יותר אבל יציב, נניח 70 ו-65, מנצח אותו.

---

## הדאטה

20 מחלקות, 1000 תמונות לכל אחת. סה"כ 20,000.

התיקייה נמצאת ב-dataset/train, והתיקיות בפנים בפורמט 00_rooster.

בנוסף הורדנו תיקיית augmentations עם שלושה סוגי עיוות: black_white, color_jitter, salt_pepper. **אבל יש בה רק 20 תמונות לכל מחלקה.** זה מעט מדי לשימוש רציני.

---

## החלטה 1: פיצול הדאטה — בוצע

לפני הכל צריך דרך למדוד. בלי סט בדיקה נפרד אין שום דרך לדעת אם משהו עובד.

**80 אחוז אימון, 20 אחוז ולידציה.** 16,000 מול 4,000.

למה 20 ולא משהו אחר. הרעש במדידה תלוי בכמות המוחלטת של תמונות הבדיקה, לא באחוז. 4,000 תמונות נותנות אי-ודאות של פחות מאחוז, וזה מספיק כדי להבחין בין שיפור אמיתי לרעש. מצד שני הירידה מ-1000 ל-800 תמונות אימון למחלקה כמעט לא מורגשת.

שלוש החלטות בתוך הפיצול:

**מפצלים כל מחלקה בנפרד (stratified).** אחרת יוצא במקרה שמחלקה אחת מקבלת 187 תמונות בדיקה ואחרת 214.

**seed קבוע.** זה החלק הקריטי. בלי seed כל הרצה מגרילה פיצול אחר, ואז כשמשווים שני מודלים משווים גם שני סטי בדיקה שונים ואי אפשר לדעת מה השתנה. עם seed קבוע אותן 4,000 תמונות נשארות בבדיקה לתמיד, וכל הפרש מגיע מהמודל בלבד.

**ממיינים לפני שמערבבים.** סדר הקבצים שמערכת ההפעלה מחזירה לא מובטח. בלי מיון, אותו seed יכול לתת פיצול שונה במחשב אחר.

**התוצר:** קובץ split_data.py, ופלט קפוא ב-dataset/split.json. שומר נתיבים בלבד, אף תמונה לא זזה.

---

## החלטה 2: מד החסינות — בהוראות, טרם הורץ

צריך מספר שאומר כמה המודל נשבר כשמעוותים תמונה.

תיקיית augmentations לא מספיקה לזה, כי 20 תמונות למחלקה זה כלום. ובנוסף, רק חלק מהן שייכות לצד הוולידציה שלנו.

**הפתרון: כותבים את שלושת העיוותים בקוד.** מפעילים אותם על כל 4,000 תמונות הוולידציה ומקבלים שלושה סטי בדיקה בגודל מלא.

התיקייה שהורדנו הופכת לתמונת ייחוס בלבד — מסתכלים עליה כדי לכוון עוצמה. שהרעש שאנחנו מייצרים ייראה כמו שלהם, לא חזק פי שניים ולא חלש פי שניים.

**התוצר המתוכנן:** קובץ data.py שמכיל את ה-Dataset, את ה-transforms, את שלושת העיוותים, ואת ה-DataLoaders.

אילוץ חשוב: הנרמול חייב להיות זהה בדיוק לזה שב-evaluate.py. Resize ל-256, CenterCrop ל-224, ונרמול עם ממוצע וסטיית תקן של ImageNet. אם נאמן עם נרמול אחר, המודל יקבל בבחינה תמונות בסקאלה שהוא לא מכיר וייפול.

---

## העיקרון המרכזי: איך בכלל משיגים חסינות

לא קיימת חסינות לכל שינוי אפשרי. מה שקיים זה משהו אחר.

רשת שראתה מגוון רחב של עיוותים בזמן האימון מפתחת נטייה להתעלם מהמראה השטחי ולהיאחז בצורה — מקור, צוואר, פרופורציות, קווי מתאר. וזו התכונה שמעבירה גם לעיוותים שלא ראתה.

כלומר לא מאמנים על עיוות ספציפי. מאמנים על **כמות ומגוון** של עיוותים, עד שהרשת מוותרת על צבע ומרקם ככלי זיהוי כי הם הפכו ללא אמינים.

**ולכן מייצרים את העיוותים בקוד ולא בקבצים.** בתיקייה שהורדנו יש עותק אחד קבוע לכל תמונה — רעש אחד שכבר הוגרל וקפא. אם מאמנים על זה, הרשת רואה את אותה תמונה מרועשת שוב ושוב ויכולה לשנן אותה. כשהעיוות מיוצר בקוד, כל epoch מגריל מחדש, והרשת רואה אינסוף גרסאות. זה מה שמאלץ אותה להתעלם במקום לשנן.

---

## מבנה הבדיקה: מניפולציה מוחזקת

הבעיה: אם נאמן על שלושת העיוותים ונבדוק על שלושתם, נקבל מספר יפה שלא מנבא כלום. הרשת פשוט למדה את מה שראתה.

**הפתרון: משאירים אחד מהשלושה בצד ולא נוגעים בו באימון בכלל.**

אם המודל עומד יפה בעיוות שמעולם לא ראה — זו עדות שהוא למד להתעלם. אם הוא נופל עליו — החסינות מזויפת.

זה המספר היחיד שמנבא את הציון האמיתי.

מועמד להוחזק: salt_pepper. שחור-לבן ושינוי צבע שניהם משנים ערכי פיקסלים בצורה חלקה, בעוד רעש הורס פיקסלים בודדים. הצלחה עליו אחרי אימון על השניים האחרים היא הראיה החזקה ביותר.

---

## הסדר קדימה

1. ✅ פיצול הדאטה — split_data.py
2. ⬜ צינור הנתונים ומדדי החסינות — data.py
3. ⬜ **מודל בסיסי בלי שום עיוות באימון.** יקבל ציון גרוע על הסטים המעוותים, וזו המטרה — בלי הבסיס הגרוע הזה לא נדע אם משהו ממה שנעשה אחר כך עזר
4. ⬜ הוספת עיוותים לאימון, ומדידה מול הבסיס
5. ⬜ בדיקה מול המניפולציה המוחזקת

---

## תזכורות טכניות

- evaluate.py מחפש את הדאטה ב-dataset/validation, והדאטה שלנו ב-dataset/train. יצטרך התאמה כשנרצה להריץ סימולציה של הבוחן
- שמות הקבצים ב-augmentations מזוהים לפי ה-hash באמצע, לא לפי המספר בהתחלה
- predict.py לא נוגעים בו
- המשקולות נשמרות בשם המדויק weights.joblib, אחרי העברת המודל ל-CPU
- ההגשה מכילה ארבעה קבצים בלבד: train.py, model.py, predict.py, weights.joblib, ובנוסף README
